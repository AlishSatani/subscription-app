query Subscriptions($first: Int, $offset: Int) {
  subscriptions(first: $first, offset: $offset) {
    nodes {
      id
      name
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
  }
}
